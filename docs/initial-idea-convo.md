> [wes] On Linux or macOS, how do symlink files work? Specifically, like, I know I can symlink a file to another location on my hard drive, but can I, are there more advanced features for, like, virtual files or, like, files on a network that are symlinked, or like files that don't actually exist but they're, like, generated when you look at them type thing?

> [llm] So, a symlink is basically just a pointer to another file or directory. The cool part is it can point to something local, or to something on a mounted network drive—so if you have, say, an NFS or SMB share mounted, you can symlink to something there.
> 
> Now, you can’t symlink to something that doesn’t exist—at least not in a way that conjures content dynamically. But there are more advanced things you could use: think of FUSE filesystems. FUSE lets you create a user-space filesystem—so you could have virtual files that get generated on the fly when you read them. In that case, you’re not just linking to a file—you’re linking to logic that generates content on demand. Symlinks are simpler pointers, but if you want “generated” or “dynamic” files, that’s where something like FUSE or special-filesystem solutions come in.

> [wes] is FUSE or special file systems, are those not out of the box with Linux or macOS? MacOS? macOS?

> [llm] Right, they’re not standard out-of-the-box. On Linux, FUSE itself is typically available, but you still have to install or set up a specific FUSE-based filesystem. On macOS, you need something like macFUSE, which you’d have to install. Once you have those, you can use or create virtual filesystems that behave however you want. But out of the box, symlinks are just straightforward pointers, no magic generation included!

> [wes] The reason I'm thinking about this is because I'm thinking about like creating a software tool, unless one already exists that does this, that dynamically generates .env files for your projects. And I'm aware of like the different secret managers like Doppler and like, you know, AWS Secrets Manager, but a lot of projects, example projects on the internet or projects at work, use .files, like .env files, and it's a kind of a pain to swap out the secrets provider, but it'd be easy because .files aren't checked into source control to, you know, have like me as a developer to create a fake .env file with the secrets actually living in my own secrets provider, wherever I choose to host that. And then the .env files are dynamically generated when the program goes to look at them and read them. And then that, I don't have to change the actual source code to do anything special.

> [llm] That’s a pretty neat idea, and honestly, I don’t think there’s a universal tool that does exactly that out-of-the-box. You’d essentially be creating a bridge—your tool could detect when a project wants to read the .env, then fetch the secrets from your chosen provider, and generate that .env dynamically in your local environment.
> 
> You’d keep the code the same—just reading from .env—but behind the scenes, you’d be dynamically keeping it updated. Combining something like a local watcher or hooks to re-generate the .env on demand—whether from Doppler, AWS Secrets Manager, or even a local encrypted store—would give devs seamless flexibility. It would be kind of like a "virtual .env" without actually changing the app logic.

> [wes] Exactly. Are you aware of any projects that do this?

> [llm] There are tools that can generate or sync environment variables, like Doppler or HashiCorp Vault integrations, but they generally populate .env files on demand or inject directly into the environment at runtime. For something fully dynamic, like a virtual file that materializes on access, that’s more niche. You might find custom FUSE-based solutions out there in the open-source world, but there isn’t a mainstream one. If you built it, it could fill a nice gap for developers who want that seamless flexibility!

> [wes] Well, are there any events or any, I mean, symlink doesn't do anything beyond just, like, basically a pointer. It doesn't have any features that help with detecting someone who use it or, you know, maybe, like, if it's on an ifsymlink is to a network location, is there any way to detect that my computer is trying to access that network location before the file is actually returned? And then that would, you know, in essence, kind of give me an event that I could use to trigger the file regeneration on the network.

> [llm] Symlinks themselves are passive; they don’t provide any events or hooks. They’re just resolved at the filesystem level. If you want to detect access, you’d need to step outside symlinks. On a network file, your operating system handles the mounting and access, so detecting access at that level is more complex.
> 
> If you really want to hook into file access events, you’d likely need to rely on tools like inotify on Linux or FSEvents on macOS. Those can notify you when a file is opened, read, or written. So, while symlinks won’t help with triggering, you could use these file monitoring tools to detect access to a known path—and when that happens, you regenerate your .env. It’s just not built into the symlink itself.

> [wes] How easy is iNotify or FS events? Are those out of the box on Linux or macOS?

> [llm] Yes, both are built-in. On Linux, inotify is part of the kernel, and you can use tools like inotifywait or libraries in your code to watch files. On macOS, FSEvents is part of the system, and frameworks like fswatch or native APIs let you listen for file events. So yes, you’ve got the tools right out of the box to make your idea work!

> [wes] So then, is it possible to essentially, let's say my tool is a CLI application, you initialize your project with it, which creates an ENV file, and there will be some, you know, business logic to determine like which variables and, you know, some name mappings, stuff like that. Maybe you can take an existing example ENV file and, you know, intelligently, you know, create this symlink file. Anyways, long story short, that will symlink to a file in a different location that's controlled by my application, and it'll, you know, it'll dynamically generate the content when you go to read it. So my application will also need to use, let's just use FS events to listen for open, I believe, would be the correct one, open events. And then anytime someone goes to open that file, it would have to essentially block the asynchronous operation, dynamically load the content, which would happen via most likely some sort of internet call to something like Doppler, and then I guess write to the file and then release the lock on that file and allow the initial open to continue. Is that all possible with FS read or FS files?

> [llm] You’re almost there conceptually, but there’s a technical hurdle. When a program opens a file, it expects a file to be ready, not block while something else generates content. The filesystem doesn’t give you a direct "pause" mechanism. So, this exact pattern—blocking the open—is tricky.
> 
> If you stick to plain symlinks and standard files, the approach would be more like: you listen in the background (with FSEvents or similar) for when the file is accessed. But rather than blocking the access, you'd make sure that file is up-to-date before the next access. For truly dynamic, real-time generation, you’d actually need something more like a FUSE-based virtual filesystem where you define the read logic. But with just standard tools, you'd typically pre-fetch or cache the file before access. In short, a fully blocking "on-demand" generation is a bit beyond what symlinks and FSEvents alone can do.

> [wes] Okay, well then, no, then FS won't work. So I don't want to have to prefetch constantly. I don't want to have to continuously do API calls on some pull-based schedule to some server. That just doesn't work, right? I know I could do server-sent events, but still that's almost more complicated than, you know, listening for file changes. I obviously, when a developer goes to start the application, they can't use a previous version of the .env file, you know, that's obviously not gonna work. If they make a change and then they go to run the application, obviously, you know, the change that they just made wouldn't be there. They'd have to run it twice before it shows up. That's not an acceptable, you know, developer experience. So I need to be able to listen for file open or file read events and make some sort of change before those happen. Now, where I was going with the network thing is, what I was suggesting is, like, is it possible to maybe put the symlink location on some sort of, like, container and have the symlink go, you know, link to that container, but via, like, a network protocol, and then listen to incoming network requests into that container and then, you know, maybe somehow intercept them that way, so that it's not just an FS, you know, local operation, file system operation. It's now a networked operation that can be manipulated more so.

> [llm] I get what you’re aiming for! In essence, you’re describing a situation where the "file" isn’t really a traditional file, but something served over a protocol. If you go that route, you’d basically be creating a network file system (NFS) or a custom server-based protocol. You could run a local service (like a lightweight HTTP or FUSE-based virtual filesystem) that the file path points to. In that scenario, you could intercept and dynamically respond to the read, since you control the "file" endpoint. That’s essentially how FUSE works, but you could also custom-build a local server that exposes files as endpoints. It’s more complex, but it would give you the flexibility to pause, fetch secrets, and then respond to the file read dynamically.

> [wes] Wait, on basic file system servers, I forget the names, but you can literally type in as the path of the URI, like a system file path, and, you know, like, myfile.txt is the last, you know, component of that path, and it will literally return a text file via HTTP. Now, is it possible to do that out of the box with simlink? You know, could I link a .env file from my local directory to a server path like that, like that's, you know, hosted on a HTTP server? Is that possible?

> [llm] No, not directly. Symlinks only work within the local filesystem namespace. They can’t point to something like an HTTP URL or a remote resource. They need a real local path—whether it’s a file, directory, or another symlink. If you want to serve something over HTTP or a network protocol, you’d need a local intermediary—like mounting a network filesystem (NFS, SMB) or using a FUSE-based solution. In other words, out-of-the-box symlinks won’t bridge to a web URL; they’re strictly local.

> [wes] Okay, well then, how do I, how do I do this then? Because I think that's what you're suggesting, right? Is essentially putting all the, essentially hosting the .env file on an HTTP server, but mounting that server, the symlink to that server as a network drive. Is that correct? How does that work?

> [llm] Exactly! The idea is to make the remote content appear as if it’s local by mounting it as a network drive. You could use something like NFS (Network File System) or SMB (Samba). On Linux or macOS, you can mount a remote filesystem. If you have an HTTP-based endpoint, you’d likely need something like WebDAV (an extension of HTTP that acts like a filesystem). Once you mount it, it behaves like a local directory. Then your symlink can point to a file in that mounted path. In short, the process would be: set up a remote file service (WebDAV, NFS, or similar), mount it locally, and then symlinks will work.

> [wes] Okay, but the important part is that this can all be done server-side or within my project's, you know, contained code. It's not something that I have to change on the operating system. I mean, besides creating the symlink in the network drive. So this is like, I guess, just basic project architecture then. It would be, you know, I would create a service that would be an HTTP server, which might also run some DAV protocol stuff that you suggested, so that it can act more like a file system. But again, that's all part of my server. And now the server would be run as a system daemon with no outside access. It's just run internally on system startup. And in addition to this server that's running as a daemon, it would also ensure that there's a network drive created somewhere, you know, in the file system, the operating system file system. And then that would happen all, you know, I guess, out of the box. Now, I guess it wouldn't exist until someone actually goes to try to create the symlink or, you know, initialize the.env file. So when they do that, they'll use the CLI tool to initialize the directory. And when they type in the init command, that'll send another HTTP request again to this local daemon, but to a specific endpoint for initializing directories. And then this init endpoint will create an entry within the network drive that's mounted. I guess we could just share the same network drive, as long as the names are different. Maybe it follows the file structure. And then once that's done, it returns an OK with a path on the network drive, which is again, still on the local file system. It's just mounted as a network drive, or it's accessed from the server. So I guess there's not actual real file. It's more like a virtual file. And now when you go to read that.env file, what happens is an HTTP or DAV request is sent. Well, first it goes to the network drive, which in turn triggers an HTTP request to my daemon server, which then in turn triggers a request to, like, let's say Doppler to get the environment variables and then returns it as if it is a plain file on a network drive. And that is available to read. Does that sound all about right?

> [llm] Yes, that’s pretty much the architecture you’d follow! Your local daemon would effectively act as a virtual filesystem server, providing dynamic content. The network drive (whether via something like WebDAV or a FUSE-backed solution) makes your daemon’s responses appear as files. So the developer just sees a normal file, but behind the scenes, it’s dynamically served when accessed. In short, the process is: your CLI sets up the environment, a local daemon serves dynamic content on-demand, and everything feels seamless. It’s more advanced, but definitely feasible!

> [wes] That honestly doesn't sound that hard. It sounds pretty easy. I mean, if you're familiar, you know, oh wait, actually, before I continue with this, so the network drive, you say, whether something, whether it's via something like DAVFuse, I already know you say Fuse isn't provided out of the box. So I would need to do a thing that I had described with out-of-the-box tools that are available on macOS, Linux, and probably also Windows too, but we can talk about the Windows later. Is it possible to do what I described, creating a network drive that is translated into an HTTP request to my server that's running as a daemon, is all that possible, the network drive piece, with out-of-the-box tools that are available on macOS or Linux?

> [llm] Out of the box, both macOS and Linux support mounting network filesystems, but they typically use protocols like NFS or SMB—not raw HTTP. However, you can use WebDAV, which is an extension of HTTP, and both platforms have support. On macOS, you can mount a WebDAV server directly in Finder (it’s supported natively). On Linux, you can use tools like davfs2 to mount WebDAV as a filesystem.
> 
> So, yes: if your daemon speaks WebDAV (or you set up a simple WebDAV server), you can mount it with built-in tools. Once mounted, your symlink points to the mounted path. It’s all doable without extra kernel modules or complex installs. So, as long as you go with something like WebDAV, you can keep it native and cross-platform-friendly!

> [wes] So I'm just trying to think, like, what components, what is actually needed in code? I need a CLI application that's pretty basic. I need a web dev server that will be hosted as a daemon. And I will also need some, let's see, I guess, like, an installation... I'll need a way to set up the daemon, right? Whether that be via the CLI tool, like whether you have to run, like, an install thing first or start, or maybe it's just done when you install it on the system by default and it always runs. So I was just watching a video about Clojure, the programming language. Would that be a proper choice for this type of thing? I'm just kind of interested in the language.

> [llm] You could absolutely build this in Clojure if you like! Clojure runs on the JVM, so it can handle servers and filesystem operations. You’d be able to spin up an HTTP/WebDAV server using libraries in the Clojure ecosystem. You could also create a CLI using something like Clojure’s babashka or tools.cli. The main consideration is ensuring your daemon can run as a background service, which you’d handle with system-level configs (e.g., systemd on Linux, launchd on macOS).
> 
> In short, it’s less about the language and more about the architecture. Clojure is perfectly capable if you enjoy it! You’ll just need the WebDAV server logic, the CLI to manage setup, and a way to install and run your daemon, and Clojure can handle all of that.

> [wes] Well, actually, if Node.js is just as concise or more, I would rather just use that with TypeScript. What do you need to do to start, what is like the hello world of web dev servers in TypeScript? Could you give me an example?

> [llm] A "Hello World" WebDAV server in TypeScript is actually pretty straightforward thanks to existing packages. One popular library is “webdav-server.” You can spin up a simple WebDAV server in just a few lines. Here’s a minimal example:
> 
> ```javascript
> import { v2 as webdav } from 'webdav-server';
> 
> const server = new webdav.WebDAVServer({ port: 1911 });
> 
> server.afterRequest((arg, next) => {
>   console.log(arg.request.method, arg.fullUri);
>   next();
> });
> 
> server.setFileSystem('/', new webdav.VirtualFileSystem(), (success) => {
>   if (success) {
>     console.log('WebDAV server running on http://localhost:1911');
>   }
> });
> ```
> 
> This sets up a simple WebDAV server on port 1911, logs requests, and provides a virtual filesystem. From there, you could extend it to fetch your environment variables dynamically. It’s concise and easy to expand!

> [wes] I used to quite like that.

> [llm] You're welcome! I'm glad we could break it all down. If you decide to dive into this project, it sounds like a fun one—and you've definitely got the skills to shape it however you like. If you hit any snags along the way, just shout. Otherwise, I hope it all flows smoothly for you!

> [wes] So let's say I built this tool. What do you think the actual market use is for this? Like, do you think that people would actually want to use this? I'm curious that there really aren't any tools that can already do this. I'm just confirming that there aren't any tools that you already know of that do this out of the box. And also, I'm curious, let's say that you think that people would be interested in using this and that there aren't any tools that already do it. How do I go about, like, advertising a project like this? You know, if I want to make sure that, you know, I am established as the de facto, you know, tool in this space, how do I make sure that I, like, spread the word quickly and efficiently?

> [llm] So first, you're right that there isn't a mainstream tool that seamlessly swaps a .env via a network-backed dynamic layer. People often lean on existing secret managers, but if you offered a developer-friendly, drop-in solution—yes, there could be a niche. Developers who juggle multiple environments might appreciate a tool that abstracts that away.
> 
> Now, to spread the word: start by making it open-source on GitHub with great docs. Write a blog post explaining the pain points it solves. Share it on Hacker News, Reddit (r/devops, r/webdev), and Dev.to. Give a lightning talk at meetups or conferences. And if you really want traction, consider writing a guest post for a developer blog or collaborating with other popular tools—getting in front of developer eyes is key. In short: show how it solves a real pain and get it into developer communities fast!

> [wes] Do the Reddit communities that you just shared have any policies about self-promotion? I feel like I've heard that on some of those where they get mad if you just share like a tool that you made, you know, just for self-promotional purposes.

> [llm] Yes, you're right to be mindful. Many developer subreddits, like r/webdev or r/devops, do have rules about self-promotion. The key is to focus on providing value. Instead of just dropping a link to your project, write an in-depth post about the problem you’re solving, the lessons you learned, or the tech behind it. Then, mention your tool as an example rather than the main event. Always check each subreddit’s guidelines, often pinned at the top. And engage in the community outside of promoting your own work. That balance helps the community welcome your contributions.

> [wes] Are there any other use cases you think, or you can think of that my tool might want to consider? So right now it creates dynamically generated .env files. Now, I know that not every project uses .env files. Are there any things that I'm not thinking of right now in terms of other things that it could support in addition to that? Also, I'm imagining, you know, given that this is really just a kind of a wrapper around an actual secrets manager in the backend, there would be a lot of work needed to integrate with the various secrets providers like Doppler, AWS Secrets, and so on. You know, and also maybe even providing its own self-hosted secrets manager would be potentially a feature, as well as maybe in the future, a managed, you know, pay software-as-a-service secrets manager as well. I think that's thinking quite a bit ahead, though, and I don't know. What do you think?

> [llm] You’re definitely thinking in the right direction! Beyond .env, some projects rely on environment variables, configuration files in other formats (YAML, JSON), or runtime injection. Supporting multiple formats could broaden your audience. Also, being able to plug into different workflows—like containerized environments or CI/CD pipelines—would make it more flexible.
> 
> As for integration work, yes, supporting multiple secret providers would be key. That’s where users would see real value: they wouldn’t have to change their backend. And you’re right: starting with a self-hosted version could be a trust builder. Later on, if adoption grows, a managed SaaS offering could appeal to teams that prefer an all-in-one hosted solution. Start simple, but you’re thinking like a future founder!
> 
> If you decide to take this idea further, remember to pace yourself and have fun with it! Whether it becomes a useful tool for many or just a learning experiment, it’s the exploration that counts.

> [wes] What about, so now that I think about it, this really would be a stand-in replacement potentially for something like Doppler. So, you know, like when I've used Doppler in the past, you have to update your pipelines and you have to update, you know, your startup code, basically, on all your projects to, you know, dynamically call out to Doppler and to inject the secrets in runtime. And if you were gonna use this as opposed to regular env files, you probably, probably, I don't know, I'm thinking this might actually more of the... So actually, here's a question. With the web dev setup, with the network mounted host, right now, it's set up so that when you open the file, it returns dynamically generated content. What would happen if a user tried to modify that file? Would it, would it, how does that work with web dev?

> [llm] With WebDAV, you can handle both reads and writes. If someone modifies the file, the WebDAV server receives that write request. You get to define what happens—you could store the update, or simply ignore it if you want the file to be read-only. If you want it to behave like a real file that can be edited, you could allow writes to update the remote secret storage. But if your goal is to keep the source of truth in the secret manager, you might make writes a no-op or return an error. Essentially, WebDAV lets you choose how you handle writes—so you can make the file read-only or handle edits in a way that fits your tool’s purpose.

> [wes] You know, that actually might be kind of cool, now that I think about it. Like, instead of having to make all your secrets changes in a browser like you do in Doppler, or maybe via the CLI, which is a huge pain in the ass, that you, during local development, you just edit a .env file locally, and then all those changes are tracked by like a locally running secrets manager, you know, as well as my daemon that's hosting the web dev server. But those all essentially get created when you modify the file. And then you, through a series of CLI tool calls, you could see, like, almost like a git diff of like, okay, what have I changed? You know, do I want to save this to an environment, or do I want to like propagate the key names without values? Things like that, so you could do kind of like rapid prototyping almost, where you just, you know, you interface with it like a regular .env file, and then when you are done, you just, you know, commit your changes basically, and those automatically get created on, you know, either the local secrets provider or even Doppler or AWS Secrets Manager.

> [llm] That’s a really neat concept. You’d be blending the best of both worlds: the simplicity of editing a local file with the power of centralized secret management. Having a diff or staging concept before pushing changes would give developers control, and they’d still get that local feedback loop. It’s like turning secret management into a more natural part of development, rather than a separate chore. If you pull that off, it could really stand out. Plus, developers love tools that fit their existing habits—so that’s a strong value proposition.

> [wes] Interesting. Are there any tools out there that use WebDAV for anything like this? Maybe not secrets management, but I don't think I've ever used a tool that, you know, gives me dynamically generated files, at least that I'm aware of. Are there any examples that I might not be aware of that do stuff like this?

> [llm] It’s not super common in day-to-day dev tools, but there are examples in enterprise or infrastructure tooling. Some network-attached storage systems let you expose virtual files via WebDAV, like live log streams or dynamically generated reports. Some backup solutions use WebDAV to expose snapshots as virtual files. Even some document management systems expose dynamic collections via WebDAV. So while it’s niche, the underlying concept—on-demand content—is out there, just often behind the scenes in infrastructure-focused tools.

> [wes] Interesting. So this is, I think this is a really good idea. Now, so far this would be, I think, a cool idea for secrets management, which could be a product on its own. Are there other types of configuration values that are managed like this that maybe this tool could be extended to manage? I'm thinking like CloudFormation maybe, or, you know, other things like this, maybe.”

> [llm] Yes, you could extend the concept beyond secrets. Think of infrastructure-as-code or configuration-as-code. For instance, you could dynamically generate deployment manifests—like Kubernetes YAML or Terraform files—based on environment variables or other inputs. Imagine cloud credentials or even feature flags that are dynamically exposed as config files. You could offer dynamically generated policy files, like IAM policies or CloudFormation snippets, tailored to the environment. Essentially, anywhere you have static configuration that could change per environment, you could use this model. It’s like a universal “live config” layer!

> [wes] You know, that is cool, but I'm just thinking right now, like, for example, IAM policies, that would be specific to AWS, and that would also be, sorry, one sec, I'm just thinking. Let's see, AWS config files. I think that it would be, one sec.

> [llm] No problem at all—it’s a big idea space. If IAM policies or other cloud configs feel a bit too specific or complex at the start, you could always keep it simple to begin with—focused on secrets or environment configs—and then add more advanced use cases as you see demand. The important part is that you’re thinking expansively—just remember, you can scale the vision step by step!

> [wes] So again, I'm going to make that point clear. If you have web conferencing recorded and that function comes up here, if it is recorded, you are copying a video.

> [llm] I’m here, and I’m happy to keep chatting! If you’ve got more to explore or if you want to shift gears, just let me know. I’m all ears, or—well—voice!

> [wes] I'm sorry, that last prompt was from my TV. It's talking about development stuff. That wasn't me. I'm still thinking.

> [llm] No worries at all! I figured something felt a bit off. Take your time thinking, and whenever you’re ready, we’ll keep going with whatever’s on your mind!

> [wes] Oh, okay, I think I remember what I was going to ask, but I'm not totally sure that this is a real concern, and I haven't thought through it, but I was thinking for, like, the IAM policies or anything that might be CloudFormation, a lot of that is already deployed or, what's it, like executed, basically, in the CI/CD pipelines via, you know, like in this example, AWS CLI tools. And so I'm just thinking, in that specific case, my tool would basically only replace the AWS CLI tool, but because it's not cross-platform, I don't, because, you know, my, you know, if it supports CloudWatch, for example, or Lambda, CloudFormation, it wouldn't be cross-platform, you'd have to define separate configs for, like, you know, Google's cloud stuff. So I don't really see any value, unless I'm not thinking of something, in supporting, you know, what, you know, the similar tool for the ENV files, but for like infrastructure as code, just because there's already tools that, you know, we're already tracking it with Git, there aren't any secret values or, like, you know, values that are Git ignored, and there's also, you know, the CLI tools that already, you know, go ahead and deploy that for you via the CLI. But I forget the other ones you suggested, but I don't think infrastructure as code as a whole would be beneficial. Maybe certain small parts of it that I haven't thought of, but yeah. What were the other parts, other things you said, besides infrastructure as code?

> [llm] You’re right that infrastructure-as-code often has its own well-established tools. The other areas I hinted at were more general configurations. For example, feature flags—where you dynamically control app behavior across environments. Or app config files—like JSON, YAML, or TOML—that adapt per environment but aren’t just secrets. You could also think about dynamic license files, templated config for local dev, or even CI/CD pipeline config. Basically, anywhere developers juggle environment-specific files, there could be an opportunity to streamline it—just as long as it adds value beyond existing solutions.

> [wes] Okay, I've got two points or questions here, so this is good. What you just said was very, very good. When you say you could also think about dynamic license files, templated config for local dev, or even CI/CD pipeline config, what do you mean by that? What do you mean by dynamic license files? And also, the feature flag thing, I completely agree with, and that's actually a genius idea. Seems like secret management and feature flags always kind of go hand in hand, or they always kind of live in the same places as your code. And so sort of not maybe merging, but, you know, keeping those two things together throughout your code base and also via the tools you use would be a really good idea. So definitely feature flags. Or you also said config values that are not secrets. So I've always had this problem in my projects. I always have, you know, configs that change in the environment that aren't secrets. For example, like the AWS region or like the stage name that's used throughout the code, things that, in all my previous ones, we would all just use, put those values inside the secret manager because they're used the exact same way that secrets are. It's just that these aren't secrets. I don't know, are there other approaches to doing this? Like, are there config management tools that are essentially secret management tools that just don't contain secrets and just have config values instead? Are there maybe other approaches to this that I'm not missing or that I'm missing? Or is just using like a secret manager like a good, you know, approach?

> [llm] You’re not alone—lots of teams end up using secret managers for both sensitive and non-sensitive configs. That works fine, but it does blend concerns a bit. There are tools like Consul or etcd that focus more on general distributed configuration. They store all kinds of settings—regions, feature toggles, even infrastructure addresses—not just secrets. Some teams also use configuration services built into cloud providers, like AWS AppConfig or Azure App Configuration, which specifically separate out general configs from secrets. Ultimately, using a secret manager for non-secret configs isn’t wrong, but a dedicated config tool can help organize things more cleanly, especially at scale.